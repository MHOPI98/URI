#include <bits/stdc++.h>
using namespace std;
int main()
{
    double a, b, c, x;
    cin >> a >> b >> c;
    cout << "NUMBER = " << a << endl;
    x = b * c;
    cout << fixed << setprecision(2) << fixed << "SALARY = U$ " << x << endl;

    return 0;
}