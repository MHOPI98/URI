#include <bits/stdc++.h>
using namespace std;
int main()
{
    double a, b, c;
    cin >> a >> b;
    c = a + b * 0.15;
    cout << fixed << setprecision(2) << fixed << "TOTAL = R$ " << c << endl;
    return 0;
}